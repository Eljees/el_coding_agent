# Аудит проекта — local_codex_lite (v3, консолидированный)

Дата: 2026-06-06 · Ветка: `test/artifact-unpack` · План согласован с Elaria (фазы 0–4 + бэклог утверждены).

Предыдущие аудиты этого дня перенесены в `archive/` (v1: `AUDIT_AND_PLAN_20260606.md`, v2: `AUDIT_20260606_v2.md`). Фазы A–D из них выполнены коммитами `52a7375` (plan A-C) и `01f4126` (phase D); этот документ фиксирует состояние **после** них.

---

## 1. Состояние на момент аудита (замеры)

| Метрика | v2 (утро) | v3 (вечер) |
|---|---|---|
| Тесты (`tests/`) | 439 passed | **507 passed** |
| Покрытие | 74% | **78%** |
| ruff check / format | clean | clean |
| mypy | 0 errors | **1 error** (`ui_runners.py:26`, unused `type: ignore` — внесено `01f4126`) |
| runner.py | 100% | 100% |
| trufflehog.py | 69% | **95%** |
| llm_client.py | 70% | **96%** |
| replay.py | 78% | **99%** |
| ui.py | 1157 строк / 14% | 1016 строк / 14% |

## 2. Актуальные дефекты

- 🔴 mypy red: `ui_runners.py:26` — unused `type: ignore`. (Фаза 2)
- 🔴 `.git/index.lock` (0 байт, stale) блокировал git-запись. **Исправлено в фазе 0** — удалён, `git fsck` чистый.
- 🟠 `skills/appsechub` тесты не собираются в `.venv`: `requests` отсутствует в dev-extras. Цифра «450 с skills/» из v2 в этом окружении невоспроизводима. (Фаза 2)
- 🟡 AGENTS.md: счёт тестов 439 устарел, фактически 507. (Фаза 2)
- 🟠 `ui.py` 1016 строк / 14% — основной остаток арх-долга. (Фаза 4)
- 🟡 Хвосты покрытия: `artifact_unpack` 64%, `cli.py` 68%, `cli_review` 69%, `cli_evidence` 72%. (Фаза 3)
- 🟠 Нет `main` на origin (только `test/*`); `main` живёт на `gitlab-new`. (Бэклог)
- Workflow-риск: репо в папке YandexDisk — источник битых локов (сегодняшний index.lock, ранее 69 lock.bak + 225 tmp_obj). **Решение Elaria: оставить как есть**, риск принят.

## 3. Чистка (фаза 1, выполнено)

Удалены локальные gitignored-кэши (~40 MB): `.mypy_cache`, `.ruff_cache`, `.pytest_cache`, `.hypothesis`, `.coverage`, все `__pycache__`, пустой `_local/`. `.claude/` добавлен в `.gitignore`. Конфликтных дубликатов `(2).pyc` нет. `.local-codex-lite/` (runtime) сохранён.

## 4. Согласованный план

- **Фаза 0** — разблокировать git: ✅ выполнено.
- **Фаза 1** — чистка мусора, консолидация audit-доков: ✅ выполнено (этот документ).
- **Фаза 2** — мелкие фиксы: mypy green (`ui_runners.py:26`), AGENTS.md 439→507, `requests` в dev-extras, зафиксировать политику `testpaths` для `skills/`.
- **Фаза 3** — покрытие: `artifact_unpack` 64→~80%, `cli.py`/`cli_review` до ~80%.
- **Фаза 4** — рефактор `ui.py`: вынести оставшиеся `cmd_*`/worker-методы в тестируемые модули, UI как тонкий event-диспетчер.
- **Бэклог (утверждён к проработке)**: `main` на origin + protection; appsechub против живого API + capability-плагин; команда трендов (дельта issues между сканами); автоген CLI-reference из парсера; `docs/INDEX.md`; coverage-gate по дельте.

Гейт после каждой фазы: `ruff check` + `ruff format --check` + `mypy` + `pytest -q` зелёные; коммит на фазу.
